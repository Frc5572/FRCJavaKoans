# The WPILib release the koans compile against. Bump this one line to move seasons,
# but check that the ejml/jackson/quickbuf versions in build.gradle still match
# allwpilib's own wpimath/build.gradle for the new tag.
wpilibVersion=2026.2.2

org.gradle.parallel=true
org.gradle.caching=true
